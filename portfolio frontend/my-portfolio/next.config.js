/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 's3.amazonaws.com', 's3-portfolio-berkay-mustafa-arikan.s3.amazonaws.com'],
  },
}

module.exports = nextConfig
